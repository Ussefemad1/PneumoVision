# PneumoVision inference service (Python 3.11, matching the training env).
#
# Torch/transformers/timm are NOT pinned here — the service imports the
# preprocessing and model classes from medpatch/, which pins them in the repo
# root requirements.txt. Install both when running the real pipeline:
#   pip install -r requirements.txt -r services/inference/requirements.txt
fastapi==0.115.6
uvicorn[standard]==0.53.0
pydantic==2.10.4
pydantic-settings==2.7.0
httpx==0.28.1
python-multipart==0.0.20
Pillow==12.3.0

# dev / test
pytest==9.1.1
pytest-asyncio==0.25.0
