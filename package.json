{
  "name": "pneumovision",
  "private": true,
  "version": "0.1.0",
  "description": "PneumoVision — multimodal clinical decision-support platform (MedPatch fusion)",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "npm run test --workspaces --if-present",
    "dev": "npm run dev --workspaces --if-present",
    "dev:demo": "node infra/scripts/dev-demo.mjs",
    "gen:ehr": "node infra/scripts/gen-ehr-constants.mjs",
    "gen:secrets": "node infra/scripts/gen-secrets.mjs",
    "audit:js": "npm audit --audit-level=high",
    "audit:py": "pip-audit -r services/inference/requirements.txt",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md,yml,yaml,css}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.6.2",
    "eslint": "^10.11.0",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^17.12.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.5.1",
    "prettier": "^3.4.2",
    "typescript": "^7.0.2",
    "typescript-eslint": "^8.18.1",
    "vite": "^8.3.1",
    "vitest": "^5.0.2"
  },
  "type": "module"
}
